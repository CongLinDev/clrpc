zookeeper:
  registry:
    address: localhost:2181
    root-path: /clrpc
  discovery:
    address: localhost:2181
    root-path: /clrpc
  monitor:
    address: localhost:2181
    root-path: /clrpc
  atomicity:
    address: localhost:2181
    root-path: /clrpc
  session:
    timeout: 5000 #ms

provider:
  address: localhost:5100
  thread:
    boss: 1
    worker: 4
    
consumer:
  address: localhost:5200
  cache:
    enable: false
  session:
    wait-time: 5000 #ms
  thread:
    worker: 4

service:
  thread:
    pool:
      core-size: 5
      max-size: 10
      keep-alive: 1000 #ms
      queue: 10
  codec:
    serialization-handler: conglin.clrpc.common.codec.protostuff.ProtostuffSerializationHandler
  cache:
    enable: false
